package com.example.feature.routineElements

// Visual model used by the UI. Keep it simple while we use decoy/in-memory data.
// Later, you can map from your database entity to this UI model.
data class RoutineElementUiState(
    val id: Long,
    val title: String
)

